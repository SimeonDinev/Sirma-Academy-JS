function comparer(x, y) {
  if (x < y) {
    console.log(y);
  }
  // else if (x === y) {
  //   console.log("They are equal!");
  // }
  else {
    console.log(x);
  }
}
comparer(2, 4);
comparer(7, 12);
comparer(-1, -5);
// comparer(-4, -4);
